{
  "name": "extensions",
  "description": "The official extension repository for moonlight",
  "devDependencies": {
    "@electron/asar": "^3.2.8"
  },
  "scripts": {
    "build": "node build.mjs"
  }
}
